#include "solver.h"

#include "rules.h"

int solve(Grid *g) {
    (void)g;
    // TODO(William): encontrar proxima celula vazia.
    // TODO(William): testar candidatos com is_valid.
    // TODO(William): aplicar backtracking com set_cell.
    return 0;
}
